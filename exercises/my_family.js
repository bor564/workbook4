// Declare an array of family names
const familyNames = ["Eileen", "Nancy", "Alex", "Tame"];

// Loop through the array and print each name
for (let i = 0; i < familyNames.length; i++) {
  console.log(familyNames[i]);
}
